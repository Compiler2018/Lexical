package TypeOfToken;

public enum Keytype {
    INT,
    DOUBLE,
    STRING,
    DO,
    WHILE,
    IF,
    ELSE,
    SWITCH,
    CASE,
    FOR,
    BOOL,
    TRUE,
    FALSE,
    CIN,
    COUT,
    CONST;
}
